# Map assets

Drop the two same-size overhead city images into this folder as:

- `stad_dull.png` — the default, muted city
- `stad_nice.png` — the colorful version revealed by captured territory

The included images are 1448 × 1086. The canvas uses that same size so they remain perfectly aligned and undistorted.
